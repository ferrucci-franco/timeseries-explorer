{
  "name": "openmodelica-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:portable": "node scripts/build-portable.mjs",
    "build:all": "npm run build && npm run build:portable"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16",
    "esbuild": "^0.25.5",
    "vite": "^5.4.19"
  },
  "dependencies": {
    "plotly.js-dist-min": "^3.5.1"
  }
}
